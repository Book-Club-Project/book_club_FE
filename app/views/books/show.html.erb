What we need:

-[:volumeId]
-[:volumeInfo][:title]
-[:volumeInfo][:authors] (array)
-[:volumeInfo][:imageLinks]
- [:volumeInfo][:description] (summary)
-[:volumeInfo][:averageRating]

maybe average rating instead of genre
